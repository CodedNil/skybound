[package]
name = "skybound"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.16.1", features = [
    # "dynamic_linking", # Don't ship with this enabled
    "wayland",
] }
smooth-bevy-cameras = "0.14.0"
avian3d = "0.3"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[profile.release]
lto = true
